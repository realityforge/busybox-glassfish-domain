# busybox-glassfish-domain

A minimal docker image that defines a single domain in the GlassFish application server.

### Usage

Example: 

    docker run -it --rm realityforge/busybox-glassfish-domain:latest

